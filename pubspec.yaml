name: inventa
description: "A Premium, Privacy-First Home Inventory App."
publish_to: 'none'
version: 1.0.1+9

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # Icons
  cupertino_icons: ^1.0.8
  
  # State Management
  flutter_riverpod: ^2.6.1
  
  # Database
  sqflite: ^2.4.1
  path: ^1.9.0
  
  # ML & AI
  google_mlkit_text_recognition: ^0.14.0
  google_mlkit_barcode_scanning: ^0.13.0
  
  # Charts
  fl_chart: ^0.70.2
  
  # Utils
  intl: ^0.20.2
  uuid: ^4.5.1
  path_provider: ^2.1.5
  image_picker: ^1.1.2
  share_plus: ^10.1.4
  
  # PDF & Printing
  pdf: ^3.11.1
  printing: ^5.14.0
  
  # Security
  local_auth: ^2.3.0
  shared_preferences: ^2.5.3
  purchases_flutter: ^9.11.0
  url_launcher: ^6.3.2
  flutter_local_notifications: ^22.2.0
  timezone: ^0.11.1
  flutter_timezone: ^5.1.0
  image: ^4.9.1
  archive: ^4.0.9
  file_picker: ^11.0.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_native_splash: ^2.4.4
  flutter_launcher_icons: ^0.13.1
  sqflite_common_ffi: ^2.4.2
  path_provider_platform_interface: ^2.1.3

flutter_launcher_icons:
  image_path: "assets/images/app_icon.png"
  
  android: true
  ios: true

flutter_native_splash:
  color: "#FFFFFF" # White background usually safer for blue icon
  image: assets/images/app_icon.png
  android_12:
    image: assets/images/app_icon.png
    color: "#FFFFFF"

flutter:
  generate: true
  uses-material-design: true
  
  # Assets will be added here loosely or specifically
  assets:
    - assets/images/
    # Embedded in PDF reports. PDF's built-in Helvetica cannot draw Vietnamese
    # at all -- see assets/fonts/README.md.
    - assets/fonts/
